<?xml version="1.0" encoding="utf-8"?>
<androidx.coordinatorlayout.widget.CoordinatorLayout
        xmlns:android="http://schemas.android.com/apk/res/android"
        xmlns:app="http://schemas.android.com/apk/res-auto"
        xmlns:tools="http://schemas.android.com/tools"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        tools:context=".MainActivity" android:id="@+id/mainCoordinator">

    <include layout="@layout/content_main" android:id="@+id/currentInclude"/>

    <include layout="@layout/content_friends"
             android:id="@+id/friendInclude"/>
    <include layout="@layout/content_daily"
             android:id="@+id/dailyInclude"/>
    <include layout="@layout/content_schedule"
             android:id="@+id/scheduleInclude"/>
    <include layout="@layout/content_profile"
             android:id="@+id/profileInclude"/>
    <com.google.android.material.appbar.AppBarLayout
            android:layout_height="wrap_content"
            android:layout_width="match_parent"
            android:theme="@style/AppTheme.AppBarOverlay" android:id="@+id/mainAppBar">

        <androidx.appcompat.widget.Toolbar
                android:id="@+id/toolbar"
                android:layout_width="match_parent"
                android:layout_height="?attr/actionBarSize"
                android:background="?attr/colorPrimary"
                app:popupTheme="@style/AppTheme.PopupOverlay"/>

    </com.google.android.material.appbar.AppBarLayout>
    <com.google.android.material.floatingactionbutton.FloatingActionButton
            android:id="@+id/fab"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_gravity="bottom|end"
            android:layout_margin="@dimen/fab_margin"
            android:foreground="@drawable/ic_add_white_24dp" android:foregroundGravity="center"/>

</androidx.coordinatorlayout.widget.CoordinatorLayout>